// // it is also an iteration loops are called.
// for (let a = 0; a < 10; a++) {
//     const element = a;
//     if (element == 5) {
//         console.log("5 is the best number")
//     }
//     console.log(element);
// }

// 
// for (let i = 0; i < 10; i++) {
    // console.log(`the tables are : ${i}`);
    // for (let j = 0; j <= 10; j++) {
        // console.log(`${i}*${j }  = ${i*j}`);
    // }
// }

let apple = ["flash","batman","chadman"]
for (let index = 0; index < apple.length; index++) {
    const element = apple[index];
    console.log(element);
}

// break and continue in loops 